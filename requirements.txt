# Dependencias del balanceador.
#
#   python3 -m venv .venv
#   ./.venv/bin/pip install -r requirements.txt

# grpcio: el balanceador es CLIENTE gRPC de las réplicas (afuera habla HTTP).
grpcio==1.83.1
# protobuf: los mensajes de contrato.proto.
protobuf==7.36.1
# health-checking: para consultar grpc.health.v1.Health de cada réplica.
grpcio-health-checking==1.83.1
